
#include "main.h"

/**
 * print_sign - gets the absolute of any number
 * @c: number to check
 * Return: positive value of any numbe
 */
int _abs(int c)
{
	if (c < 0)
		return ((-1 * c));
	return (c);
}
