#include <stdio.h>
#include <string.h>

int main(void)
{
	char *line = NULL, *token, *delim = " ", *str;

	size_t len = 0;

	printf("$ ");
	if ((getline(&line, &len, stdin)) != -1)
	{
		printf("%s", line);
		for 
		printf ("Dbufg\n");


	}
	else
		printf("Error\n");

	return (0);
}
