#include <stdio.h>

/**
  * main - prints all single digit numbers of bs 10
  * Return: zero
  */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}
